---
 - name: This play is used to stop ec2 instance
   hosts: localhost
   connection: local
   gather_facts: fasle
   tasks:
   - name: stopping ec2 instance
     ec2:
      instance_ids: 
      region: ap-south-1
      state: stopped
      wait: yes
